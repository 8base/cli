
export const DEFAULT_ENVIRONMENT_NAME = 'Master';

export enum EnvironmentCloneModeType {
  FULL = 'FULL',
  SYSTEM = 'SYSTEM'
}

export enum MigrationOutputModeType {
  TS = 'TS',
  YAML = 'YAML'
}
